% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ex_cls.R
\name{ex_cls}
\alias{ex_cls}
\title{Sampling error when estimating the mean using a cluster sampling design}
\usage{
ex_cls(C, n_real, sd_est, m, N = Inf, parameter = FALSE)
}
\arguments{
\item{C}{Level of confidence; 0 <= C <= 1.}

\item{n_real}{A positive integer representing the real sample size (the number of clusters sampled); n_real > 0.}

\item{sd_est}{Estimated standard deviation ; sd_est > 0.}

\item{m}{Average cluster size (number of elements), either of the population -preferred- or of a preliminary sample; m > 0.}

\item{N}{A positive integer indicating the population size (id est, the total number of clusters). Defaults to infinite.}

\item{parameter}{Type TRUE if you do know the populations SD in sd_exp, or type FALSE (default) if they are estimates.}
}
\value{
This function returns the sampling error when using a cluster sampling design without replacement to estimate the mean, given the sample size.
}
\description{
Sampling error when estimating the mean using a cluster sampling design
}
\details{
The function looks for the value of 'E' that, given the real sampling size, fits:
\deqn{n = \frac{N \cdot \text{sd}^2}{\frac{N \cdot E^2 \cdot m^2}{Z^2} + \text{sd}^2}}
where 'sd' is parameter 'sd_est', and 'Z' is the quantile of the two-tailed normal distribution function, compatible with the chosen confidence level 'C'.
If 'sd_exp' is unknown, the t-student is used instead of the normal distribution.
}
\examples{
ex_cls(C = 0.95, n_real = 31, sd_est = 15000, m = 8, N = 500, parameter = TRUE)
}
